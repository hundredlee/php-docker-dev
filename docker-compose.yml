version: '2'

services:
  db:
    image: mariadb:10.1
    environment:
      MYSQL_ROOT_PASSWORD: "root"
      MYSQL_DATABASE: "groups"
      MYSQL_USER: "groups"
      MYSQL_PASSWORD: "123456"
    volumes:
      - db:/var/lib/mysql
  
  php:
    build:
      context: ./services/php
      dockerfile: Dockerfile
    volumes:
      - /Users/hundredlee/PHPStorm/Forchange/groups:/mnt/groups
  
  # console:
  #   build:
  #     context: ./services/console
  #     dockerfile: Dockerfile
  #   volumes_from: 
  #     - php
  #   tty: true
  redis:
    build:
      context: ./services/redis
      dockerfile: Dockerfile
    ports:
      - "6379:6379"
  web:
    image: nginx:1.11.1
    ports:
      - "8080:80"
    depends_on:
      - php
    volumes_from:
      - php
    volumes:
      - ./services/web/config:/etc/nginx/conf.d/


volumes:
  db:
    driver: local